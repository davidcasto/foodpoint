# Food Point

A starter Angular website for Food Point.

## Development

Install dependencies and run the dev server:

```bash
npm install
npm run start
```

Open `http://localhost:4200/` in your browser.

## Build

Build the project for production:

```bash
npm run build
```

## Notes

This app uses Angular 16 and includes a simple Food Point homepage with menu, about, and contact sections.
